namespace Constants
{
    internal class Program
    {
        static void Main(string[] args)
        {
            /*
             * When we say "constants are factored out at compile-time and baked into the calling site," it means
             * that the value of the constant is determined at compile-time and then embedded directly into the
             * code wherever the constant is used. This is different from variables, whose values are looked up 
             * at runtime.
             */
        }
    }
}
